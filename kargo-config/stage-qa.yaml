apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: qa
  namespace: kargo-poc

spec:
  requestedFreight:

    - origin:
        kind: Warehouse
        name: myapp

      sources:
        stages:
          - dev

  promotionTemplate:
    spec:
      steps:
        - task:
            name: myapp-promotion
          as: promotion
